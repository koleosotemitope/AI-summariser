# AI-summariser

Image-to-text summariser.

## Usage

1. Install dependencies:
   ```bash
   pip install -r requirements.txt
   ```
2. Run:
   ```bash
   python summariser.py /path/to/image.png --sentences 3
   ```

> Note: OCR requires the Tesseract binary to be installed on your system.
